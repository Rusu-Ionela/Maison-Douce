# Example environment variables for backend
# Copy this to `backend/.env` and fill real values (do NOT commit real secrets)

PORT=5000
BASE_CLIENT_URL=http://localhost:5173
BODY_LIMIT=10mb

MONGODB_URI=mongodb://127.0.0.1:27017/maison_douce

JWT_SECRET=change_me_local
JWT_EXPIRES=7d

STRIPE_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CURRENCY=usd

# BASE_SERVER_URL is useful when backend needs to call itself (e.g. rezervare -> calendar patch)
BASE_SERVER_URL=http://localhost:5000
PORT=5000
BASE_CLIENT_URL=http://localhost:5173

# Alege UNA dintre baze și păstreaz-o peste tot
# MONGODB_URI=mongodb://127.0.0.1:27017/maison_douce
MONGODB_URI=mongodb://127.0.0.1:27017/torturi

JWT_SECRET=CHANGE_ME_IN_LOCAL_ENV

STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

BODY_LIMIT=10mb
